# Build Backlog — Pharmacy Bill Agent

Derived from `pharmacy_bill_agent_prd_v3.md`. Organized into phases in build order — mostly linear, with parallelizable stories called out. Each story references the PRD section it implements. Check items off as you go; this file is the source of truth for "what's left," not the PRD (the PRD is *what to build*, this is *in what order*).

**Budget:** ~2 weeks solo. Day estimates below assume ~10 real build days once demo/video/submission time is reserved at the end.

---

## Phase 0 — Accounts & Environment (Day 1, ~half day)

Nothing downstream can start until this is done. Do it in one sitting.

- [x] **T01** — Google Cloud account confirmed (developer's personal Gmail, not shown here), billing account linked (`My Billing Account`, 01221D-846796-941715), standard new-account Free Trial active — ₹28,694 (~$300) credit, 0 used, expires 2026-11-17. This is the generic GCP trial, **not** the hackathon's $150 credit — those are separate and stack. Still worth claiming the hackathon one via the Resources-tab promo code for extra headroom, but not blocking
- [x] **T02** — Created project `pharmacy-bill-agent`, billing linked, set as active gcloud config
- [x] **T03** — Enabled: aiplatform, gmail, drive, run, firestore, pubsub, cloudscheduler, cloudtrace, logging, secretmanager APIs
- [x] **T04** — Firestore Native mode created, region `asia-south1` (Mumbai)
- [x] **T05** — Twilio account created, WhatsApp sandbox activated. Sandbox number `+17372508034`, pharmacist's personal number (redacted here — a real phone number, not committed; see Firestore `config/pharmacist` / Secret Manager) joined via the `join <code>` message. Sandbox session expires after 72h inactivity (PRD §9 risk table) — re-join before the demo recording if stale. Superseded by T41's pivot to Meta's WhatsApp Cloud API — the Twilio account/sandbox is no longer used by any code path.
- [x] **T06** — `git init`, first commit, GitHub repo created and pushed — `origin` is `https://github.com/nandu8/pharmacy-bill-agent.git`, branch `main` up to date with `origin/main`. If the repo is private, still need to share with `testing@devpost.com` + `cloudhackathons@google.com` before submission (PRD §17, tracked as T67)
- [x] **T07** — Add `.gitignore` covering secrets, `.env`, credentials, `__pycache__`, local venvs — before anything else gets committed (also covers raw `samples/`, see T14)

---

## Phase 1 — Gmail OAuth & Credential Security (Day 1–2, ~half day)

Blocks all ingestion work. Do this early because of the 7-day token trap (PRD §9).

- [x] **T08** — OAuth consent screen configured (Google Auth Platform), scopes added: `gmail.readonly`, `gmail.send`, `drive`. Desktop OAuth client created (`credentials.json`, gitignored, not committed)
- [x] **T09** — Published to Production via the Audience tab
- [x] **T10** — Secret Manager: `gmail-refresh-token` populated (version 1, minted via `scripts/gmail_oauth_setup.py`). `twilio-auth-token` and `twilio-account-sid` (new secret, not originally listed but needed alongside the token) both populated, version 1 each. Vertex AI uses the Cloud Run-attached service account directly, no downloaded key needed.
- [x] **T11** — Created `pharmacy-agent-sa@pharmacy-bill-agent.iam.gserviceaccount.com`, scoped to: `aiplatform.user`, `datastore.user`, `pubsub.editor`, `secretmanager.secretAccessor`, `logging.logWriter`, `cloudtrace.agent`, `run.invoker` — no project-editor role. (Gmail/Drive access is via the pharmacist's own OAuth consent in T08/T09, not this service account — personal Gmail accounts can't use domain-wide delegation.)

---

## Phase 2 — Real Data & Pharmacist Input (Day 1–2, parallelizable with Phase 0–1)

- [ ] **T12** — Get remaining/updated sample files from your father when available (more invoices per vendor, ideally with overlapping SKUs, to get real price-history signal — PRD §14 gap)
- [ ] **T13** — Get daily bill volume and vendor count from your father (PRD §14 open question #1 — the only unresolved item in the PRD)
- [x] **T14** — Write the sanitization/scrubber script: replace vendor names, GSTINs, licence numbers, phone numbers, addresses with synthetic values, preserving invoice-number shape and all prices/quantities/dates exactly (PRD §10) — run on all samples before anything touches the repo or a recording. `scripts/scrub_samples.py`, output in `samples_sanitized/` (committed); raw `samples/` stays gitignored. Verified zero residual PII across all 5 PDFs and byte-identical CSV/XLS output except the one Format B vendor-name field.

---

## Phase 3 — Parsing & Normalization (Day 2–3)

The data layer everything else depends on. Build against the real samples in `samples/`.

- [x] **T15** — `detect_format`: inspect file bytes (not extension) — distinguish CSV/delimited text, BIFF-signature binary, PDF. `src/pharmacy_agent/formats/detect.py`, keyed on the literal BIFF2 BOF magic bytes + `%PDF` + CSV header sniffing.
- [x] **T16** — `parse_csv`: handle Format A (79-column ERP export) and Format B (H/D/F row-type, VAT/TS naming) — both dialects confirmed from real samples. `src/pharmacy_agent/formats/parse_csv.py`.
- [x] **T17** — `parse_xls`: xlrd-based reader for the BIFF2 `.xls` files — confirmed working 5/5 on samples (PRD §7.3). `src/pharmacy_agent/formats/parse_xls.py`; regression test asserts openpyxl/default-engine still fail on these files (so detect_format's routing reason stays true).
- [x] **T18** — `parse_pdf_vision`: Gemini multimodal read of PDF tax invoices, via Vertex AI (`google-genai` SDK). `src/pharmacy_agent/formats/parse_pdf_vision.py`; builds a `Bill` directly (no separate normalize step — PDF has no raw-row schema). Confirmed against real sanitized PDFs: matches the xls twin's hand-verified line (`SILVEREX SSD CREAM 20GM`) and correctly resolves the `PH-26-49832` reconciliation case (13-item/2959.00 version). Model `gemini-3.5-flash` only serves from the Vertex AI `global` location for this project, not a region like `us-central1` — 404s there. Needed local `gcloud auth application-default login` (ADC) to test; production path (Cloud Run service account) is unaffected. `tests/test_parse_pdf_vision.py` (2/2 passing, live Vertex AI calls).
- [x] **T19** — Normalizer: map every format onto the unified schema (§10), including the generic `tax_component_1/2_label/rate/amount` fields — don't hardcode CGST/SGST. `src/pharmacy_agent/normalize.py`.
- [x] **T20** — Per-line arithmetic validator: `taxable_value = qty×rate − discount`, `line_total = taxable_value + tax1 + tax2` — verify against real sample math (already hand-verified in the PRD; port that into code + a test). `src/pharmacy_agent/validate.py` + `tests/test_parse_and_normalize.py` (10/10 passing against `samples_sanitized/`).

---

## Phase 4 — Firestore Data Layer (Day 3, parallelizable with tail of Phase 3)

- [x] **T21** — Firestore collections `bills`, `purchase_ledger`, `agent_runs` per PRD §10. `src/pharmacy_agent/firestore_client.py`: client + collection accessors (project `pharmacy-bill-agent`, database `(default)`, region `asia-south1` from T04). Firestore collections are virtual (exist only while non-empty), so "creation" is verified via a live write/read/delete round-trip per collection in `tests/test_firestore_client.py` (4/4 passing) rather than a provisioning call. Added `google-cloud-firestore` to `requirements.txt`.
- [x] **T22** — `record_purchase`: write normalized bill to `purchase_ledger`, keyed on `invoice_no` + `vendor` (idempotent — PRD §7.10). `src/pharmacy_agent/purchase_ledger.py`: one ledger doc per line item, doc ID is a sha256 hash of vendor+invoice_no+item_name+batch_no so retries/resumes overwrite rather than duplicate; `normalize_item_key` slugifies item names for future cross-vendor lookups (T24/T33). `tests/test_purchase_ledger.py` (4/4 passing, live Firestore writes).
- [x] **T23** — `check_duplicate`: invoice number + vendor lookup; distinguish "already resolved" from "same number, different content" (reconciliation case). `src/pharmacy_agent/check_duplicate.py`: queries `bills` by vendor+invoice_number, fingerprints line items (item_name/batch_no/quantity/rate/taxable_value/line_total) + total_amount to classify `NEW` / `DUPLICATE` (identical content) / `RECONCILIATION` (same number, different content — routes to T36) — surfaces the matched bill's `status` so callers can tell an already-resolved repeat from an in-flight one. `tests/test_check_duplicate.py` (3/3 passing, live Firestore).
- [x] **T24** — `lookup_vendor_history`: prior invoices/pricing for a vendor+item from the ledger. `src/pharmacy_agent/lookup_vendor_history.py`: queries `purchase_ledger` by vendor + `normalized_item_key`, sorted most-recent-first by `purchase_date`, optional `limit` (for the "last four invoices" pattern in PRD §7.4). `tests/test_lookup_vendor_history.py` (4/4 passing, live Firestore).
- [x] **T25** — Seed script: load real sample invoices into `purchase_ledger`; separately generate clearly-labelled synthetic history for price-drift demo data (PRD §10 — keep the two sources distinguishable in the data itself, e.g. a `seeded: true` flag). `scripts/seed_purchase_ledger.py`: `seed_real_samples` records all real Format B/C samples plus the authoritative PH-26-49832 CSV version (skips PDFs — twins of data already captured; skips the conflicting 14-item twin — reserved for T36); `seed_synthetic_price_history` writes 4 months of synthetic AMLODIPINE 5MG/SUMMIT PHARMA rate history. Added a `seeded: bool` field to every `purchase_ledger` doc (`record_purchase`'s new `seeded` param, default `False`) and to `VendorHistoryEntry`. Idempotent re-run confirmed (55 real + 4 synthetic docs live in Firestore). `tests/test_seed_purchase_ledger.py` (2/2), plus updated `tests/test_purchase_ledger.py` (5/5 passing).

---

## Phase 5 — Agent Core Loop (Day 4–5)

The reasoning engine. Get one full bill resolving end-to-end before adding more tools.

- [x] **T26** — ADK project scaffold; wire Gemini 3.5 Flash as the reasoning model. `src/pharmacy_agent/agent/model.py`: `build_agent()`/`build_model()` construct an ADK `Agent` (LlmAgent) with a `VertexGlobalGemini` subclass pinning the underlying `google.genai.Client` to the Vertex AI `global` location (same 404-on-region issue as T18). Added `google-adk` to `requirements.txt`. `tests/test_agent_model.py` (2/2 passing) — one live turn against real Vertex AI confirms the model is actually reachable, not just constructible.
- [x] **T27** — Define the toolbox (PRD §7.2) as ADK tool functions — start with the Phase 3/4 tools already built. `src/pharmacy_agent/agent/tools.py`: ADK `FunctionTool`-compatible wrappers for `detect_format`, `parse_csv`, `parse_xls`, `parse_pdf_vision`, `lookup_vendor_history`, `check_duplicate`, `record_purchase` (the other 6 PRD §7.2 tools depend on Phase 6/7/9 infra and aren't wired yet). Tools read/write the run's session state (`tool_context.state`) rather than take file bytes or a parsed `Bill` as an LLM-visible arg; `_impl` functions take a plain `state` dict so they're unit-testable without a real `ToolContext`. Verified the generated function-calling schemas are well-formed for all 7 (`FunctionTool._get_declaration()`). `tests/test_agent_tools.py` (12/12 passing, live Firestore + one live Gemini PDF call).
- [x] **T28** — Agent loop: turn-by-turn tool selection, observation, decision (PRD §7.1). `src/pharmacy_agent/agent/loop.py`: `run_bill(file_bytes, vendor_hint="")` seeds a fresh ADK session's state with the bill and drives the ADK `Runner` turn by turn, recording each turn's tool call(s) into `tool_call_history` (the PRD §8 trace). Terminal-state interpretation (resolved/pending_pharmacist/pending_vendor) is T29 — a run here just ends when the model stops calling tools. `tests/test_agent_loop.py` (1/1 passing) — a live, real multi-turn Gemini run over the clean SUMMIT PHARMA sample resolves end-to-end (detect → parse → record) with zero validation issues.
- [x] **T29** — Terminal states: `resolved` / `pending_pharmacist` / `pending_vendor` — wire the loop to stop cleanly in each case. `src/pharmacy_agent/agent/terminal.py`: a `finish(status, summary)` tool the model must call as its last action (ADK's "no more tool calls" signal only means the model stopped, not what it concluded); `record_bill_result` persists the outcome to `bills`, keyed on a deterministic `vendor::invoice_no` hash (same idempotency pattern as `purchase_ledger`), using the `invoice_number`/`status` field names `check_duplicate.py` already reads. `loop.py` falls back to `pending_pharmacist` if the model ever stops without calling `finish` (PRD §7.10: no bill silently dropped). `tests/test_agent_terminal.py` (4/4); `test_agent_loop.py` extended to assert the live run ends via `finish` → `resolved` and the `bills` doc is written.
- [x] **T30** — Turn cap guardrail: park and notify on exhaustion rather than looping forever (PRD §7.10). `loop.py`: `run_bill(..., max_turns=15)` breaks the run (via `contextlib.aclosing` on the ADK event stream) the instant `turn_count` reaches `max_turns` without a `finish` call yet — an unconditional hard bound, independent of `finish`, so a misbehaving model can't run forever. On trip: parks `pending_pharmacist`, appends a "turn cap exceeded" finding, and logs a warning (stand-in for the real `notify_pharmacist` WhatsApp call, which is T41). Live test forces `max_turns=1` against a bill that genuinely needs several turns, so the cap is guaranteed to trip regardless of tool-call order — confirmed `turn_count == 1`, status `pending_pharmacist`.
- [x] **T31** — **Milestone check:** one clean sample bill (e.g. Summit Pharma CSV) goes in, resolves automatically, writes to `purchase_ledger`, with no anomalies. This is Demo Bill 1 — get it working before anything else. Confirmed live: `tests/test_agent_loop.py::test_run_bill_processes_a_clean_format_b_bill_end_to_end` strengthened to assert `record_purchase` was actually called and each line item's `purchase_ledger` doc actually exists in Firestore (not just an inferred `resolved` status) — SUMMIT PHARMA/SPH12474 (`PSPH12474.CSV`) resolves end-to-end with zero validation issues. Full suite green (50/50).

---

## Phase 6 — Validation & Investigation Logic (Day 5–6)

- [x] **T32** — Rate vs MRP plausibility check. Extended `src/pharmacy_agent/validate.py::validate_line` (not a new tool — this check runs automatically inside the existing arithmetic validation, same as T20, and surfaces through every parse tool's `validation_issues`). Flags `rate > mrp` (above the retail ceiling) and `rate < mrp * 0.05` (implausibly low, likely a decimal/data error); threshold calibrated against real sample rate:MRP ratios (0.20–0.86 observed, see `pharmacy_bill_agent_prd_v3.md` S7.4). `tests/test_validate.py` (5/5 passing). Full suite green (55/55).
- [x] **T33** — `cross_check_other_vendors`: same molecule, other vendors, market-shift vs. vendor-error signal. `src/pharmacy_agent/cross_check_other_vendors.py`: queries `purchase_ledger` by `normalized_item_key`, excludes the vendor under investigation, restricts to a configurable window (default 60 days) ending at `as_of_date`; per other vendor with ≥2 in-window entries, compares earliest vs latest rate and flags "moved" at a ≥5% rise. Returns `MARKET_MOVEMENT` (any other vendor moved), `NO_MOVEMENT` (none did — the "conclusive no market movement" signal PRD S7.5 condition 2 needs), or `INSUFFICIENT_DATA` (no other vendor has ≥2 in-window points). `tests/test_cross_check_other_vendors.py` (4/4 passing, live Firestore). Full suite green (59/59).
- [x] **T34** — Price deviation check against vendor history (runs on seeded data from T25). `src/pharmacy_agent/check_price_deviation.py`: `check_price_deviation` compares a bill line's current rate against the vendor's most recent prior invoice for the same item (via `lookup_vendor_history`, T24), flagging a 10%+ move as `DEVIATION_DETECTED` (PRD S7.4 worked examples cite 26-27% moves); also reports `prior_invoice_count` and a `confirmed` flag (>=3 prior invoices) so downstream dispute-gating (T39, PRD S7.5 condition 1) can tell a backed deviation from a thin-history one. `tests/test_check_price_deviation.py` (4/4 passing, live Firestore). Full suite green (63/63).
- [x] **T35** — `find_related_document`: locate the same invoice number in another format/email. `src/pharmacy_agent/find_related_document.py`: given a target invoice number + vendor + a list of candidate (filename, bytes) documents, detects each candidate's format, parses with the existing per-format parser, and matches by digit *suffix* rather than exact equality (Format A/C's assembled invoice_no, e.g. `I152516`, doesn't carry the full branch/series prefix the vendor's PDF prints, e.g. `260027300152516` — confirmed live against the real `002652...` xls/pdf twin). Unparseable candidates are skipped, not raised. Feeds both the S7.3 recovery ladder and T36 reconciliation (finds the authoritative PDF among candidates). `tests/test_find_related_document.py` (7/7 passing; two live Vertex AI PDF-vision calls against real sanitized twins — `PH-26-49832`'s authoritative PDF and the `002652...152516` xls/pdf pair, whose live-confirmed invoice_no `260027300152516` is reused as a test constant). Full suite green (70/70).
- [x] **T36** — Reconciliation logic: same invoice number, conflicting content → treat PDF as authoritative, flag the discrepancy (PRD §7.3/§7.4) — Demo Bill 3. `src/pharmacy_agent/reconcile.py`: `reconcile(bill, candidates)` returns the in-hand bill directly if it's already the PDF; otherwise finds the vendor's PDF twin among `candidates` via `find_related_document` (T35) and diffs line-by-line, matching by `normalize_item_key` + batch_no (not raw item_name — a Gemini vision re-read won't reproduce a CSV's exact text). No PDF twin among candidates → `authoritative_found=False`, caller falls back to `ask_pharmacist` (T41, not wired yet). Actually staging the resolved version is T49 (Drive) — out of scope here, this only returns the decision. `tests/test_reconcile.py` (4/4, live Vertex AI PDF-vision calls) confirms against real `PH-26-49832` data: the 14-item/3268.00 conflicting CSV diffs cleanly against the vendor's authoritative PDF (13-item/2959.00), while the 13-item/2959.00 CSV matches it exactly with zero discrepancies. Full suite green (74/74).
- [x] **T37** — Vendor-silence check (Cloud Scheduler-triggered, no incoming email) — PRD §7.8. `src/pharmacy_agent/check_vendor_silence.py`: `check_vendor_silence(vendor, as_of_date, ...)` estimates a vendor's typical billing cadence as the median gap between its own past invoice dates in `purchase_ledger`, then flags `SILENT` if more than 2x that gap has elapsed since the last one, `INSUFFICIENT_DATA` below `min_invoices` (default 3) or no history at all, else `ON_CADENCE`. Detection logic only, one vendor at a time — the actual Cloud Scheduler trigger that loops over vendors and calls this on a cadence is deploy/infra work (same kind as T50), not built here. `tests/test_check_vendor_silence.py` (4/4 passing, live Firestore). Full suite green (78/78 — see T36 for the last live-Gemini full run; this task's tests don't touch Vertex AI).

---

## Phase 7 — Dispute & Communication (Day 6–7)

- [x] **T38** — `email_vendor`: resend-request mode and dispute mode — recipient always from the trusted vendor directory, never parsed from document content (PRD §7.10 guardrail). `src/pharmacy_agent/vendor_directory.py`: new `vendor_directory`/`config` Firestore collections (hash-keyed, same pattern as purchase_ledger) hold vendor→email and the pharmacist's contact — `email_vendor` takes a vendor *name*, never a raw address, so a malformed attachment can't redirect a send. `src/pharmacy_agent/gmail_client.py`: Gmail API send via Secret Manager creds — added a `gmail-oauth-client` secret (client_id/secret, sourced from T08's `credentials.json`) alongside the existing `gmail-refresh-token`, since Cloud Run has no local file to read it from and personal Gmail can't use the service account (T11). `src/pharmacy_agent/email_vendor.py`: two modes, always CCs the pharmacist, dedup logged to `email_log` keyed on vendor+reference+mode so a retry never re-sends (§7.10 "at most once per invoice per mode"). `scripts/seed_vendor_directory.py` seeds the 3 known sample vendors + pharmacist contact, all pointed at a placeholder address until real vendor emails are known. `tests/test_vendor_directory.py` (4/4), `tests/test_gmail_client.py` (2/2, live Gmail send), `tests/test_email_vendor.py` (3/3, live sends + dedup). Full suite green (87/87).
- [x] **T39** — Dispute gating: implement the four conditions from PRD §7.5 (≥3 prior invoices, conclusive cross-vendor check, ₹500 floor, no duplicate dispute). `src/pharmacy_agent/dispute_gate.py`: `check_dispute_gate(vendor, invoice_no, price_deviation, cross_vendor_check, disputed_amount, ...)` takes T34's `PriceDeviationResult` and T33's `CrossVendorCheckResult` directly, checks the ₹500 floor, and reads T38's `email_log` collection for an existing dispute — one authorize/deny decision with a `failed_conditions` list (e.g. `unconfirmed_deviation`, `cross_vendor_check_not_conclusive`, `below_amount_floor`, `dispute_already_sent`) that doubles as the findings to hand `ask_pharmacist` on a fallback (T41, not wired yet). Doesn't call `email_vendor` itself — that wiring is the agent loop's job once `ask_pharmacist`/`notify_pharmacist` exist. `tests/test_dispute_gate.py` (9/9 passing, live Firestore for the duplicate-dispute check). Full suite green (96/96).
- [x] **T40** — `DISPUTE_REQUIRES_APPROVAL` config flag (default `false`) — PRD §7.5. `src/pharmacy_agent/config.py`: `dispute_requires_approval()` reads the `DISPUTE_REQUIRES_APPROVAL` env var at call time (not cached at import), so a Cloud Run redeploy with the var flipped takes effect without a code change; unset/falsy defaults to `False`. Not yet wired into the agent loop — that happens once `ask_pharmacist` (T41) exists to give the "on" branch somewhere to send its one-tap confirmation. `tests/test_config.py` (13/13, no live infra needed). Full suite green (109/109).
- [x] **T41** — WhatsApp integration: `notify_pharmacist` (outbound) and `ask_pharmacist` (outbound + suspends run). Built against Twilio first, but abandoned it after a real dead end: Trial-account API restrictions blocked all sending -> upgrading revealed a Trust Hub KYC requirement (~48h review) -> completing KYC still didn't work (sandbox channel broke post-upgrade) -> Twilio's own Console then required a full WhatsApp Sender registration through Meta Business Manager anyway. Pivoted to Meta's WhatsApp Cloud API directly instead -- less total work, free Test WhatsApp Business Account, no business verification. `src/pharmacy_agent/meta_whatsapp_client.py`: low-level send via the Graph API (`POST /{phone_number_id}/messages`), Bearer token from Secret Manager (`meta-whatsapp-access-token`), phone number id via `META_WHATSAPP_PHONE_NUMBER_ID` env var (not sensitive). `src/pharmacy_agent/secrets_client.py`: new shared Secret Manager accessor (factored out of `google_oauth.py`'s private one), strips trailing whitespace -- PowerShell's `'value' | gcloud secrets versions add ... --data-file=-` pipes a trailing CRLF on Windows, which broke both the Twilio and Meta credentials until this fix. `src/pharmacy_agent/pharmacist_whatsapp.py`: `notify_pharmacist`/`ask_pharmacist`, recipient always resolved from the trusted `config/pharmacist` Firestore doc (new `whatsapp` field, `vendor_directory.py`), dedup via `whatsapp_log` (vendor+reference+mode, same pattern as `email_log`). Wired as ADK tools in `agent/tools.py`, added to `agent/loop.py`'s toolbox with kickoff-message guidance to call notify before finishing resolved and ask before finishing pending_pharmacist. WhatsApp's 24h business-initiated-message window (platform-wide, not vendor-specific) means a business-sent template alone doesn't open two-way messaging -- the recipient must reply first; confirmed live end-to-end after the pharmacist replied. Tests mock the HTTP client (`tests/test_meta_whatsapp_client.py`, `tests/test_pharmacist_whatsapp.py`, `tests/test_agent_tools.py`, `tests/test_agent_loop.py`'s `_stub_pharmacist_whatsapp`) rather than sending live on every run. Full suite green (158/158).
- [ ] **T42** — Inbound WhatsApp webhook — separate entrypoint, receives replies
- [ ] **T43** — **Milestone check:** Demo Bill 2 — seeded overcharge triggers investigation → dispute email sent → pharmacist notified, zero human involvement

---

## Phase 8 — Durable Pause & Resume (Day 7–8)

- [ ] **T44** — Serialize run state to `agent_runs` on `ask_pharmacist`/`pending_vendor` (bill_id, line items, findings, tool-call history, open question, correlation key) — PRD §7.6
- [ ] **T45** — Resume Cloud Run entrypoint: match inbound reply to correlation key, rehydrate state, continue the loop from the paused turn
- [ ] **T46** — Wire the same resume mechanism to vendor resend replies (`pending_vendor` case)
- [ ] **T47** — **Milestone check:** Demo Bill 4 — park a bill, wait, reply on WhatsApp days (or minutes) later, confirm it resumes correctly and `pending_pharmacist → resolved` on Firestore

---

## Phase 9 — Drive Staging & Ingestion Pipeline (Day 8–9)

- [x] **T48** — Google Drive API integration: per-vendor folder structure. Factored the shared OAuth credential loading out of `gmail_client.py` into `src/pharmacy_agent/google_oauth.py` (`load_credentials(scopes)`) so Drive can reuse the same refresh token (T08/T09 consent already covers `drive` scope) without duplicating the Secret Manager plumbing; `gmail_client.load_credentials()` now delegates to it. `src/pharmacy_agent/drive_client.py`: `get_root_folder_id`/`get_vendor_folder_id` are find-or-create against a root folder ("Pharmacy Bill Agent") with one subfolder per vendor — idempotent, so repeated ingestion never creates duplicate folders. Byte-for-byte staging into these folders is T49, not built here. `tests/test_drive_client.py` (3/3, live Drive API — creates real folders under a distinct test root name, trashed in cleanup); `tests/test_gmail_client.py` re-verified green after the refactor. Full suite green (112/112). T41-T43 skipped for now (blocked on Twilio account/sandbox setup, deferred per T05/T10) — moved to Phase 9, which has no Twilio dependency.
- [x] **T49** — `stage_file`: byte-for-byte copy, original never modified (PRD §7.9). `src/pharmacy_agent/stage_file.py`: uploads raw bytes via `MediaInMemoryUpload` (`application/octet-stream`, no re-encoding) into the vendor's folder from T48; idempotent on a `staged_files` Firestore doc keyed on the same sha256(vendor::reference) pattern as `purchase_ledger`/`bills`/`email_log`, so a retried call returns the existing `drive_file_id` instead of re-uploading (S7.10). `tests/test_stage_file.py` (1/1, live Drive upload + download-back verifies the round-tripped bytes are exactly identical, plus a repeat call proves dedup). Full suite green (113/113).
- [ ] **T50** — Install/confirm Drive Desktop sync on the pharmacy machine (real-world step, not code — PRD §14 #4) — **blocked: needs physical access to the pharmacy machine, deferred**
- [x] **T51** — Gmail `watch` + Pub/Sub push subscription — replaces polling (PRD §9). Provisioned the `gmail-notifications` Pub/Sub topic (`gcloud pubsub topics create`) and granted `gmail-api-push@system.gserviceaccount.com` (Google's documented Gmail-push publisher, not this project's service account) `pubsub.publisher` on it. `src/pharmacy_agent/gmail_watch.py`: `start_watch()` registers/renews the push subscription (~7-day expiry, same trap as the OAuth token in T10) via `users.watch()`; `stop_watch()` cancels it. No consumer wired to the topic yet — that's T52, which needs an actual deployed Cloud Run endpoint to push to (none exists yet in this project; same "code now, deploy-time wiring later" split as T37's Cloud Scheduler trigger). `tests/test_gmail_watch.py` (2/2, live: registers a real push subscription on the developer's personal Gmail account, then stops it in teardown). Full suite green (115/115).
- [x] **T52** — Cloud Run trigger wired to the Pub/Sub push — new email → agent loop starts. `src/pharmacy_agent/gmail_history.py`: `fetch_new_attachments` resolves the Gmail history delta since a Firestore-persisted watermark (`config/gmail_watch`) into `{message_id, sender, filename, bytes}` per parseable attachment (.csv/.xls/.pdf) — needed since Cloud Run instances are stateless between pushes. `src/pharmacy_agent/ingest.py`: `handle_pubsub_push` decodes the notification, diffs the history delta, calls `run_bill` per attachment, advances the watermark; at-least-once delivery is safe because every downstream write is already idempotent (S7.10). `src/pharmacy_agent/app.py`: FastAPI push endpoint (`/pubsub/push`) verifying the Pub/Sub OIDC bearer token against `PUBSUB_PUSH_AUDIENCE`; offloads `handle_pubsub_push` via `asyncio.to_thread` since `run_bill` does its own internal `asyncio.run()`. `Dockerfile` added. Deployed to Cloud Run (`pharmacy-bill-agent`, asia-south1, `pharmacy-agent-sa`, `--no-allow-unauthenticated`); provisioned a dedicated `pubsub-push-invoker` service account (granted `run.invoker` on the service + `iam.serviceAccountTokenCreator` from Pub/Sub's service agent) and wired the `gmail-notifications-push` push subscription to it. `scripts/start_gmail_watch.py` registers the watch and seeds the Firestore baseline in one step (run once after each deploy/watch-renewal). Found and fixed two real deploy-time bugs along the way (see `firestore_client.py` and `requirements.txt` comments): a Firestore client version-skew bug reproducible only under Cloud Run's compute-engine credentials, and `/healthz` being a reserved path on `*.run.app` domains intercepted by GFE before reaching the container (renamed to `/health`). `tests/test_gmail_history.py` (2/2, live), `tests/test_ingest.py` (5/5, Firestore-live with a stubbed Gmail service), `tests/test_app.py` (5/5). Full suite green (128/128).
- [x] **T53** — **Milestone check:** send a real test email with a sample attachment, confirm it triggers the full pipeline end-to-end without manual intervention. Sent a real email to the developer's personal Gmail account with the real `PSPH12474.CSV` sample attached (via `gmail_client.send_email_with_attachment`) — confirmed via Firestore document timestamps (not just log-reading) that Gmail's push notification reached the deployed Cloud Run service, `ingest.handle_pubsub_push` pulled the attachment via the history delta, and the agent loop ran end-to-end unsupervised: `purchase_ledger` doc `create_time`/`update_time` 08:09:30 UTC, `bills` doc (status `resolved`) 08:09:34 UTC — both fresh writes, ~16s after the Gmail push arrived, zero manual steps between sending the email and the resolved state landing in Firestore.

---

## Phase 10 — Observability & Status Page (Day 9–10)

- [x] **T54** — Export ADK trace output to Cloud Trace / Cloud Logging (PRD §8). `src/pharmacy_agent/telemetry.py`: `setup_tracing()` registers a global OTel `TracerProvider` exporting via `OTLPSpanExporter` pointed at Cloud Trace's native OTLP endpoint (`telemetry.googleapis.com`), authenticated with ambient Google credentials (`AuthMetadataPlugin`) — the currently-recommended path per the opentelemetry-operations-python migration guide, since the dedicated `opentelemetry-exporter-gcp-trace` package (`CloudTraceSpanExporter`) is deprecated; its v1 read counterpart also turned out to depend on a legacy "trace bucket" resource this project doesn't have, so the live test verifies the exporter's own SUCCESS/FAILURE return value instead of reading the trace back. Cloud Trace's OTLP ingestion requires a `gcp.project_id` resource attribute or every span is rejected (discovered live, not documented) — set explicitly since only Cloud Run's resource detector would supply it automatically. `setup_cloud_logging()` attaches a JSON-formatting log handler (`CloudLoggingFormatter`) stamping the active span's trace id via `logging.googleapis.com/trace`, so Cloud Run's automatic stdout-to-Cloud-Logging capture nests each log line under its Cloud Trace span. `agent/loop.py`: `run_bill` now wraps the whole run in one outer `process_bill` span so every turn's ADK-internal spans (call_llm/execute_tool) share a single trace id, captured via `current_trace_id()` and threaded through `AgentRunResult.trace_id` and `terminal.py::record_bill_result`'s new `trace_id` param onto the `bills` doc (PRD §10 schema field, for the status page T56 to link to). `app.py` calls `setup_tracing()`/`setup_cloud_logging()` at import so non-bill-run request logs (auth failures etc.) are structured too. Added `opentelemetry-exporter-otlp-proto-grpc` to `requirements.txt`, pinning `opentelemetry-api`/`-sdk` to `1.44.0` (one minor ahead of google-adk's declared `<=1.42.1` bound, empirically confirmed compatible) since the OTLP exporter requires it. `tests/test_telemetry.py` (7/7, one live Cloud Trace OTLP export); `tests/test_agent_terminal.py` extended (2 new tests for trace_id storage); `tests/test_agent_loop.py`'s T31 milestone test extended to assert a real 32-hex trace_id is captured and persisted on the `bills` doc. Full suite green (137/137 — one run showed a live-Gemini nondeterminism flake in the unrelated T31 test, tool-call ordering only, confirmed unrelated to this change and passing on rerun). **Correction during T57:** the OTLP approach described above turned out to be undeployable — `opentelemetry-exporter-otlp-proto-grpc`'s `opentelemetry-proto` dependency hard-requires `protobuf<7.0`, directly conflicting with the `protobuf==7.35.1` pin T52 already needed for the Cloud Run Firestore fix (confirmed via a from-scratch venv install: `pip install -r requirements.txt` hit a hard `ResolutionImpossible`, invisible locally since the dev venv only ever saw incremental `pip install` calls against an already-populated environment, never a fresh resolve). Reverted `telemetry.py`/`requirements.txt` to the originally-deprecated `opentelemetry-exporter-gcp-trace` (`CloudTraceSpanExporter`) after all — it writes through the `google-cloud-trace` client, which shares Firestore's own protobuf/google-api-core stack, so it has no such conflict, and its write path was already confirmed live (`SpanExportResult.SUCCESS`); only its *read* counterpart (used to self-verify during development) was broken, which the test already worked around. `tests/test_telemetry.py` updated to match (7/7, still green).
- [x] **T55** — Confirm per-bill reasoning chains are retrievable and distinguishable (short chain vs. longer investigation chain). Found a real gap first: `agent/tools.py`/`loop.py` only wired the Phase 3/4 tools (T27's note) -- `check_price_deviation` (T34) and `cross_check_other_vendors` (T33) existed as standalone functions but were never added to the agent's toolbox, so no code path could actually produce a longer "investigation" chain. Flagged to the user, who chose to wire them now (no separate task ID). `agent/tools.py`: added `check_price_deviation`/`cross_check_other_vendors` ADK tool wrappers (same `_impl(state, ...)` pattern as the existing tools). `agent/loop.py`: added both to `_TOOLS`; the kickoff message gained investigation guidance -- first attempt ("only check when there's a real reason to") let the model exhaustively check_price_deviation every line of a 10-item clean bill and blow the turn cap (a real regression, caught by rerunning the full suite per the TDD workflow); fixed with a hard, checkable rule instead of soft guidance: only investigate price on bills with <=3 line items. `tests/test_agent_tools.py`: 4 new unit tests for the two wrappers. `tests/test_agent_loop.py`: new live test `test_run_bill_takes_a_longer_investigation_chain_on_a_confirmed_price_deviation` runs a clean 10-item bill and a dedicated single-line-item bill with a seeded ~27% price rise (PRD S7.4 worked-example magnitude) side by side, confirming the investigation bill calls `check_price_deviation` (the clean one doesn't), needs strictly more turns, ends `pending_pharmacist` (no other-vendor data to confirm market movement, so it can't autonomously resolve), and gets a distinct Cloud Trace `trace_id` from the clean run -- the short-vs-long, retrievable-per-bill distinction PRD S8 asks for. Full suite green (142/142 across two runs; one unrelated pre-existing flake in `test_ingest.py`'s baseline test, a race against the live production Cloud Run service sharing this same Firestore project, confirmed unrelated and passing in isolation).
- [x] **T56** — Build the judge-facing status page (PRD §7.11): read-only Cloud Run view, lists bills with vendor/timestamp/status, links to trace. `src/pharmacy_agent/status_page.py`: `list_bills` reads the `bills` collection and returns `BillSummary`s (vendor, invoice_number, status, findings, trace_id, updated_at from the Firestore snapshot's own `update_time` metadata rather than a redundant stored field), sorted most-recent-first in Python (query-level `order_by` needs a stored field; fine at this page's scale). `render_status_page` hand-builds a minimal HTML table -- every field is `html.escape`d before being placed in the page, since vendor/invoice/findings text ultimately originates from a vendor's own uploaded document (same untrusted-content guardrail as PRD S7.10, applied here against stored-XSS rather than tool-call injection); a bill with a `trace_id` links to the Cloud Trace console (`cloud_trace_url`), one without (older bills predating T54, or a `pending_vendor` run) falls back to an em-dash. Wired as `GET /status` in `app.py` -- deliberately the one route with no auth check at all (unlike `/pubsub/push`), since T57 needs it reachable without GCP IAM. `tests/test_status_page.py` (6/6, live Firestore + an XSS-escaping test); `tests/test_app.py` gained a reachability test. Visually confirmed against real production Firestore data (3 real `pending_vendor` bills from earlier ingest tests rendered correctly, including one real T54 trace_id link). Full suite green (149/149).
- [x] **T57** — Deploy the status page, confirm it's reachable without GCP IAM access. The service was deployed `--no-allow-unauthenticated` (T52), which locks out every route at the Cloud-Run-IAM layer regardless of app-level checks — a judge with no IAM literally can't reach `*.run.app` at all. Flagged the tradeoff to the user (single service made fully unauthenticated vs. a second dedicated status-only service); chose the former — simpler, one service, and `/pubsub/push` already does its own independent OIDC-audience verification in code (`app.py::_verify_pubsub_push`), so the Cloud-Run-IAM lock was only ever defense-in-depth there, not the sole guard. Redeployed `pharmacy-bill-agent` (same image/service account/env vars as T52, `gcloud run deploy --allow-unauthenticated`) — this redeploy is also what surfaced the T54 dependency conflict (see the correction note on T54) since it required a genuine fresh-environment `pip install`, not a locally-mutated venv. Confirmed live post-deploy: `GET /status` and `/health` both return `HTTP 200` with zero auth headers from an unauthenticated `curl` (both the canonical `-456258899964` URL and the original `-6kcq76bwhq-el` hash URL used as the Pub/Sub push audience); `POST /pubsub/push` still returns `401` with no bearer token, confirming its own app-level check holds independent of the now-removed IAM lock. Pub/Sub push subscription's endpoint still resolves correctly post-redeploy.

---

## Phase 11 — Memory & Adaptation (Day 10, can slip if time is tight)

- [ ] **T58** — Write pharmacist resolutions back to Firestore, feed into future validation context (PRD §7.7) — lowest priority if the schedule slips; the four demo bills don't strictly require this to be visible on camera

---

## Phase 12 — Demo Prep (Day 11–12)

- [ ] **T59** — Seed final price-history data for the demo (synthetic, clearly labelled per T25)
- [ ] **T60** — Dry-run all four demo bills end-to-end, confirm timing against the §11 beat sheet
- [ ] **T61** — Pre-stage Bill 4's parked state in Firestore before the recording session (legitimate setup, not an edit — PRD §11)
- [ ] **T62** — Draft the architecture diagram as a real asset (not the PRD's ASCII sketch) — PRD §17
- [ ] **T63** — Write `README.md` with spin-up instructions (local + cloud deploy) — PRD §17, required even if judges don't run it
- [ ] **T64** — Rehearse the ~4-minute script; confirm re-auth checklist (Gmail token, Twilio sandbox join) is fresh on recording day (PRD §9, §15)

---

## Phase 13 — Recording & Submission (Day 13–14)

- [ ] **T65** — Record the demo as a single continuous, unedited take (PRD §11)
- [ ] **T66** — Finalize repo: confirm no secrets committed, confirm `.gitignore` caught everything, push
- [ ] **T67** — If repo is private, share with `testing@devpost.com` and `cloudhackathons@google.com`
- [ ] **T68** — Write the Devpost submission text (description, features, tech stack, data sources, findings/learnings) — PRD §17
- [ ] **T69** — Submit: video, repo URL, status-page URL, architecture diagram, write-up
- [ ] **T70** — *(Optional, bonus only)* Public blog/video post about the build, stating it was made for this hackathon
- [ ] **T71** — *(Optional, bonus only)* Social post with `#AllThingsAgenticHackathon`

---

## Notes on sequencing

- **Phases 0–2 can overlap** — account setup, OAuth, and pinging your father for samples/volume numbers don't block each other.
- **Phase 5's milestone (T31) is the first real checkpoint** — a single bill resolving end-to-end. Don't move on to anomaly logic until this works, or you'll be debugging two layers at once.
- **Phases 6–9 are the bulk of the build** and are mostly sequential because each demo bill depends on the previous phase's tools existing.
- **Phase 11 (memory/adaptation) is the one thing safe to cut** if the schedule slips — nothing in the four-bill demo strictly requires it.
- Reserve **Phases 12–13 as non-negotiable** — PRD, video, README, and diagram are worth as much in judging as the code itself (60% of scoring is Architectural Discipline + Demo/Production Readiness combined).
