pandas>=2.2
xlrd>=2.0
pymupdf>=1.24
pytest>=8.0
google-auth==2.56.3
google-auth-oauthlib>=1.2
google-api-python-client>=2.140
google-genai>=1.0
google-cloud-firestore==2.28.1
google-cloud-secret-manager>=2.20
google-adk>=1.0
fastapi>=0.110
uvicorn[standard]>=0.30
opentelemetry-exporter-otlp-proto-grpc>=1.30

# Pinned to match google-adk's declared opentelemetry-api/-sdk range (T54):
# opentelemetry-exporter-otlp-proto-grpc pulls in opentelemetry-sdk~=1.44.0,
# one minor ahead of adk's stated <=1.42.1 upper bound. Empirically confirmed
# compatible (tests/test_agent_model.py, tests/test_telemetry.py both green)
# -- pinned explicitly so a future `pip install` doesn't silently drift.
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0

# Pinned exact (T52): newer patch releases of these four (resolved fresh
# during the Cloud Run --source build) reproduce a genuine
# "Invalid database id %28default%29" Firestore error under Cloud Run's
# compute-engine-metadata credentials specifically -- not reproducible
# locally under user OAuth ADC. Versions below are the last confirmed-good
# combination; bump deliberately and redeploy-test before loosening.
google-api-core==2.34.0
google-cloud-core==2.6.1
protobuf==7.35.1
proto-plus==1.28.3
