pandas>=2.2
xlrd>=2.0
pymupdf>=1.24
pytest>=8.0
google-auth==2.56.3
google-auth-oauthlib>=1.2
google-api-python-client>=2.140
google-genai>=1.0
google-cloud-firestore==2.28.1
google-cloud-secret-manager>=2.20
google-adk>=1.0
fastapi>=0.110
uvicorn[standard]>=0.30
requests>=2.30

# T54: exports ADK's OTel spans to Cloud Trace. NOT the standard OTLP
# exporter (opentelemetry-exporter-otlp-proto-grpc) -- that pulls in
# opentelemetry-proto, which hard-requires protobuf<7.0, directly
# conflicting with the protobuf==7.35.1 pin below (confirmed live via a
# from-scratch venv install: pip's resolver fails outright on that
# combination). opentelemetry-exporter-gcp-trace instead writes through the
# google-cloud-trace client, which shares google-cloud-firestore's own
# protobuf/google-api-core stack, so it has no such conflict. Its own
# declared bounds (opentelemetry-api/-sdk ~=1.30, i.e. >=1.30,<2.0) are loose
# enough to coexist with google-adk's <=1.42.1 ceiling without a fight.
opentelemetry-exporter-gcp-trace==1.15.0

# Pinned exact (T52): newer patch releases of these four (resolved fresh
# during the Cloud Run --source build) reproduce a genuine
# "Invalid database id %28default%29" Firestore error under Cloud Run's
# compute-engine-metadata credentials specifically -- not reproducible
# locally under user OAuth ADC. Versions below are the last confirmed-good
# combination; bump deliberately and redeploy-test before loosening.
google-api-core==2.34.0
google-cloud-core==2.6.1
protobuf==7.35.1
proto-plus==1.28.3
