[project]
name = "pharmacy-bill-agent"
version = "0.1.0"
description = "Autonomous pharmacy bill intake, verification & dispute agent (All Things Agentic Hackathon)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
